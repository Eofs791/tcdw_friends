{
  "name": "friends",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "preview": "bun run index.ts",
    "deploy": "bun run index.ts --deploy",
    "healthcheck": "bun run healthcheck.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "smol-toml": "^1.6.0"
  }
}
